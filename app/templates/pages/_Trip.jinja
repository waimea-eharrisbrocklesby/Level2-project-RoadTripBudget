```jinja
{% extends 'pages/_base.jinja' %}

{% block content %}

<header>
    <a href="/">Edit</a>
    <a href="/add_activity/{{ trip.id }}">Add Activity</a>

    <form method="POST" action="/delete_trip/{{ trip.id }}" style="display:inline;">
        <button type="submit">Delete Trip</button>
    </form>
</header>

<div class="trip-details">

    <h2>{{ trip.Trip_Name }}</h2>

    <p>Budget: ${{ trip.Trip_Budget }}</p>

    {% set namespace = namespace(total_cost=0) %}

    {% for activity in activities %}
        {% set namespace.total_cost = namespace.total_cost + (activity.Activity_Price | float) %}
    {% endfor %}

    <h3>Budget Summary</h3>

    <div style="font-size: 32px; font-weight: bold; margin: 20px 0;">
        ${{ namespace.total_cost }} / ${{ trip.Trip_Budget }}
    </div>

    <p>Total Activity Costs: ${{ namespace.total_cost }}</p>

    <p>Remaining Budget: ${{ (trip.Trip_Budget | float) - namespace.total_cost }}</p>

    <h3>Activities:</h3>

    {% for activity in activities %}

    <div class="activity">

        <h4>{{ activity.Activity_Name }}</h4>

        <p>Location: {{ activity.Activity_Location }}</p>

        <p>Hours: {{ activity.Activity_Houers }}</p>

        <p>Price: ${{ activity.Activity_Price }}</p>

        <p>{{ activity.Activity_info }}</p>

        <form method="POST" action="/delete_activity/{{ activity.id }}">
            <button type="submit">Delete Activity</button>
        </form>

    </div>

    {% else %}

    <p>No activities for this trip.</p>

    {% endfor %}

</div>

{% endblock %}
```
